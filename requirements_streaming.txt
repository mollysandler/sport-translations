# Lightweight requirements for the streaming server (CPU-only, API-based).
# No GPU, no torch, no pyannote — just external API clients.

fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12.0
deepgram-sdk>=6.0
elevenlabs>=2.32
deep-translator==1.11.4
httpx>=0.28
numpy>=1.26
python-dotenv>=1.0
