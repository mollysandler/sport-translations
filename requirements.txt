# requirements.txt
#
# Recommended Python: 3.12 (qwen-tts supports >=3.9, but Qwen3-TTS docs recommend 3.12).
#
# IMPORTANT (PyTorch):
# - CPU/Mac: `pip install -r requirements.txt` should work.
#
# System dependencies (not pip):
# - ffmpeg (required by moviepy & pydub for mp3/m4a handling)

# --- Core numerical/audio ---
numpy==1.26.4
scipy==1.12.0
soundfile==0.12.1

# --- PyTorch (pin torch + torchaudio to the SAME version to avoid ABI mismatch) ---
torch==2.2.2
torchaudio==2.2.2

# --- Utilities ---
python-dotenv==1.0.1

# --- Audio / video I/O ---
pydub==0.25.1
moviepy==2.2.1

# --- ASR ---
faster-whisper==1.2.1

# --- Diarization ---
pyannote.audio==3.1.1
huggingface-hub>=0.20,<1.0

# --- Translation ---
deep-translator==1.11.4
transformers>=4.46,<5
sentencepiece>=0.2.0

# --- TTS ---
elevenlabs==2.32.0
qwen-tts==0.0.5
